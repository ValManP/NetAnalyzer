language: java
jdk: oraclejdk8
install: true
cache:
  directories:
  - $HOME/.m2
script:
  - mvn clean install
  - mvn cobertura:cobertura
  - mvn checkstyle:check
env:
  - LD_LIBRARY_PATH=$TRAVIS_BUILD_DIR/classes:$LD_LIBRARY_PATH
after_success:
  - bash <(curl -s https://codecov.io/bash)
notifications:
  email: false
